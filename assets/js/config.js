/* ═══════════════════════════════════════════════════════════
   Налаштування сайту.
   🛑 СЮДИ НІКОЛИ НЕ КЛАДЕМО ТОКЕНИ І ПАРОЛІ.
   Репозиторій публічний, цей файл читає будь-хто у браузері.
   ═══════════════════════════════════════════════════════════ */

window.TK = {
  /* Адреса, куди форма шле заявку.
     Це Cloudflare Worker (безкоштовний проміжний сервіс) — саме він,
     а не сайт, знає токен Telegram-бота. Код воркера: /worker/
     Поки порожньо — форма чесно каже подзвонити. */
  formEndpoint: "",

  phone: "+447575051013",
  instagram: "https://www.instagram.com/t.k.performance",

  /* Ідентифікатор картки в Google (Place ID, виду ChIJ...).
     Потрібен, щоб кнопка «Усі відгуки» відкривала одразу вкладку
     відгуків, а не огляд: за назвою й адресою Google цього не вміє.
     Як дізнатись — worker/../README або спитати Вову.
     Порожньо — кнопка веде на картку компанії, як і раніше. */
  googlePlaceId: "",

  /* Робочі години у форматі [початок, кінець] у хвилинах від опівночі.
     null = вихідний. Індекс = день тижня (0 = неділя). */
  hours: {
    0: null,
    1: [540, 1080], 2: [540, 1080], 3: [540, 1080], 4: [540, 1080], 5: [540, 1080],
    6: [540, 780]
  },

  timeZone: "Europe/London"
};
