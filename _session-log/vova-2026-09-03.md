# Журнал — 2026-09-03 · день перший

**Проєкт заведено.** Робота велась із чату проєкту CSTL NEWS; далі — окремі чати.

## Що зроблено

1. **Репозиторій підключено**, був порожній (0 комітів).
2. **Скіли: 9 → 27.**
   - Спершу перенесено з `CSTL_NEWS` 9 скілів + 5 команд.
   - 🪤 **Пастка:** сім скілів у `CSTL_NEWS` лежать **симлінками** (ярликами) з
     `.claude/skills/` у `.agents/skills/`. Пряме копіювання дало б **биті ярлики** —
     скіли мовчки не працювали б. Копіював через `cp -RL`. Симлінків у `main`: **0**.
   - Вова дав джерела: `emilkowalski/skills` (12 скілів, мав 7) і
     `leonxlnx/taste-skill` (13 скілів, мав **0**). Догружено 18.
   - Звірка версій **diff**, не на око: 6 із 7 скілів Еміла побайтово ідентичні;
     `animate` відрізнявся одним рядком — узято версію автора.
   - Зіткнень імен немає (у 8 скілів тека ≠ `name:`, але дублів `name:` — 0).
3. **Документація проєкту** — `CLAUDE.md`, `NOW.md`, `SKILLS_README.md`.
4. **Команди** `/startuem` і `/finish` написані під цей проєкт (не скопійовані).
5. **Очищено від CSTL:** `fix.md` і `qa-explore.md` переписані; `audit.md`
   переписаний; `mockup.md` — приклади замінено. **`new-file.md` видалено** —
   він був на 100% про стек CSTL (`sw.js`, `bundle.js`, `STATIC_ASSETS`).
   Напишемо заново, коли оберемо стек.

## Бізнес (зі знімка Google від Вови)

T&K Performance · 12 Arun Business Park, Bognor Regis, PO22 9SX, UK ·
+44 7575 051013 · Instagram `@t.k.performance` ·
Пн-Пт 09:00–18:00, Сб 09:00–13:00, Нд зачинено.
**Тільки BMW**, акцент на ремонті двигунів і ТО. Клієнт: місцеві + ентузіасти BMW.
**Головна дія сайту:** заявка → менеджер передзвонює.

## 🛑 Знайдені пастки (записані в `NOW.md`)

- **Репозиторій ПУБЛІЧНИЙ** — перевірено через GitHub API, не припущено.
- `brandkit` і `imagegen-frontend-web` **генерують зображення**, а генератора
  картинок у Claude Code немає.
- `gpt-taste`, `image-to-code`, `stitch-design-taste` — під інші інструменти.
- **Пряме протиріччя:** `minimalist-ui` («жодних градієнтів і тіней») проти
  `high-end-visual-design` (задає конкретні тіні). Напрямок називати словами.
- `node --check` мовчки повертає **0** на файлах з `import`/`export` — прибрано
  з `/audit` і `/fix` як брехлива перевірка.

## ➡️ Далі

Стек не обрано (моя рекомендація — ванільний + GitHub Pages, розбір у `NOW.md`).
Бриф і напрямок дизайну — перше завдання. Потрібні від Вови справжні фото з майстерні.

---

# Сесія 2 — 2026-09-03 · гілка `claude/startuem-4b83sw`

**Старт через `/startuem`.**

- `git config` — коміти підписані Вовою.
- `git fetch origin` — `origin/main` на `cdc179b` (документація проєкту).
  Гілка `claude/startuem-4b83sw` = `main`, розбіжностей **нема**.
- Прочитано обовʼязкове: `CLAUDE.md`, `NOW.md`, `SKILLS_README.md`.
- Стан підтверджено: **коду сайту нуль**, стек не обрано.

## ➡️ Чекаю рішень Вови

1. Стек (рекомендація — ванільний HTML/CSS/JS + GitHub Pages).
2. Куди йдуть заявки з форми.
3. Мова сайту: лише англійська чи ще українська.

## Роби: перша версія сайту

**Вова дав рішення і логотип:** ванільний + GitHub Pages · заявки в Telegram ·
англійська + українська. Слова Вови: *«я просто хочу побачити як ти це бачиш,
як ти це реалізуєш. Якщо мені не буде подобатися, тобі просто скину сайт в
посилання і зробимо все так як в них з нашими данними»*.

### Логотип
Прийшов PNG-квадрат: білий текст на чорному. Обробив: фон зроблено прозорим
(альфа = найяскравіший канал, поріг 8 через шум компресії), поля обрізано,
кольори повернуто un-premultiply. З нього ж — favicon, apple-touch-icon,
og-image. Пізніше перевів у WebP **lossless** — перевірив `ImageChops.difference`,
відмінності від оригіналу **немає**; lossy q92 давав середнє відхилення 26/255,
тому відкинув.

### Що зроблено
`index.html` · `assets/css/{style,fonts}.css` · `assets/js/{config,i18n,main}.js`
· `worker/` (Cloudflare Worker + інструкція) · `robots.txt` · `sitemap.xml` ·
`.nojekyll`.

### 🛑 Рішення, яке довелось пояснити Вові одразу
Токен Telegram-бота **не можна** класти в код: репозиторій публічний, GitHub
Pages — статика без сервера, увесь JS видно у браузері. Тому проміжна ланка —
**Cloudflare Worker**, токен у його секретах. Написав воркер і інструкцію.

### 🪤 Пастки, знайдені по ходу

1. **Archivo не має кирилиці.** Виявив, порахувавши `@font-face` у CSS від
   Google: 18 блоків, з них Archivo лише latin/latin-ext/vietnamese. Українські
   заголовки впали б на системний шрифт. Рішення: **Manrope тільки для
   кирилиці** через `unicode-range` — англомовний відвідувач її не вантажить
   узагалі (браузер бере шрифт лише під наявні на сторінці символи).
2. **Вкладений `backdrop-filter`.** Мобільне меню просвічувало наскрізь при
   `background: rgba(8,8,10,.98)`. Причина: меню лежить усередині шапки, яка
   теж має `backdrop-filter` — Chromium у такому разі не малює фон дочірнього
   елемента. Замінив на суцільний колір. **Побачив тільки на скріншоті**, у
   числових перевірках усе було «правильно»: opacity 1, background .98.
3. **`scroll-behavior: smooth` ламає програмний скрол у тесті.** Знімок усієї
   сторінки виходив напівпорожній: `IntersectionObserver` не встигав показати
   блоки. Це був баг **тесту**, не сайту — вимкнув smooth на час обходу.
4. **Мій же скрипт контрасту брехав.** Показав 4 «провали» — виявилось, він
   рахував напівпрозорий `rgba(255,255,255,.02)` як суцільний білий. Переписав
   зі змішуванням шарів за альфою → усі 47 пар проходять AA.
5. **Справжні провали контрасту таки були:** `--txt-3` 3.85:1 на картці (норма
   4.5 для дрібного тексту) і фірмовий червоний на номерах секцій 3.82:1.
   Завів окремий `--red-txt` #ff3355 (5.17:1); фірмовий #e4002b лишився на
   кнопках, де білий на ньому дає 4.85:1.
6. **Два однакові CTA підряд** на телефоні: під відкритим меню одразу видно
   червону кнопку з героя. Додав затемнення сторінки + блокування скролу.
7. `node --check` знову **мовчки повернув 0** на ES-модулі воркера — перевіряв
   справжнім парсингом як модуля.

### Перевірка (Chromium, не на око)
Десктоп 1440 і мобільний 390: **0 помилок консолі**, 0 горизонтального скролу,
перемикання мови і памʼять вибору після перезавантаження, валідація форми,
меню із затемненням, статус «відчинено» за часом Лондона, усі **47 пар
колір/фон проходять WCAG AA**. Вага: **311 KB, 11 запитів**.

### ➡️ Чекає Вови
1. Вердикт по дизайну — заходить чи робимо за прикладом.
2. **Фото з майстерні** — 4 плейсхолдери порожні.
3. Підключити Telegram (~15 хв за `worker/README.md`).
4. Вичитати англійські тексти — особливо перелік двигунів і послуг:
   чи все це справді робите.

**Відгуків не вигадував** — секції немає навмисно.

## Публікація на GitHub Pages

Сайт живий: **https://volodymyr221.github.io/T-K-Performance/**
PR #1 злито squash-мержем у `main`.

### 🪤 Pages не вмикається з workflow

Перший деплой упав:
`Create Pages site failed — Resource not accessible by integration`.

Автоматичний токен GitHub Actions **не має права створювати Pages site** —
це вимагає прав адміністратора, і `permissions: pages: write` цього не дає.
`actions/configure-pages` з `enablement: true` тут безсилий.

**Разова дія власника:** Settings → Pages → Source = **GitHub Actions**.
Після цього той самий крок пройшов, змінювати нічого не довелось.
Запустити workflow вручну теж не зміг — `run_workflow` і `rerun` дали 403.
Деплой запустив пушем у `main`.

### ⏱ Перше розгортання — 8.5 хвилин, наступні — 28 секунд

Крок `Deploy` тривав 12:27:22 → 12:35:51. **Я поспішив назвати це
зависанням:** поле `updated_at` запуску оновлюється рідко, тому здавалося,
що все стоїть. Скріншот Вови показав живий прогрес-бар і артефакт 579 KB.
**Урок: `list_workflow_jobs` (статус кроків) достовірніший за `updated_at`.**

### 🔒 Що не пішло в публічний доступ

Workflow збирає `_site` і викладає лише `index.html`, `assets/`,
`robots.txt`, `sitemap.xml`. Інакше на адресу сайту потрапили б `CLAUDE.md`,
`NOW.md` і ці журнали.

⚠️ Пошта Вови лежить у `.claude/commands/startuem.md`, репозиторій
публічний. Була там з першого дня. Запропонував замінити на заглушку —
рішення за ним.

⚠️ Проксі середовища блокує `github.io` — живу сторінку пальцем не
натиснув. Успіх підтверджено станом деплою, не оглядом сайту.

---

## Правки за підсумком показу

Вова: *«все дуже круто вийшло»*. Попросив дві зміни.

### 1. Шапка на телефоні
Було все притиснуто вліво. Стало три зони: логотип ліворуч · мова
посередині · дзвінок і бургер праворуч. Сітка `1fr auto 1fr`, перемикач
мови винесено з `.hdr__side` окремим елементом.
Перевірено: при ширині 390 центр перемикача 195 — **відхилення 0**.

### 2. Фото робіт
Вова дав 4 фото. Вставлено в його порядку, кадровано 4:3, 1000×750,
WebP + JPEG через `<picture>`.

### 🔴 Номерні знаки замазано — рішення прийняв сам

На фото були номери: `LG14 DXW`, `YC11 WFV`, `VX63 AOF`, `YY62 MTV`, а ще
чужі Mitsubishi `WJ61 PVL` і Ford на фоні. У Британії реєстраційний номер —
**персональні дані**: через DVLA за ним виходять на власника. `CLAUDE.md`
прямо забороняє персональні дані клієнтів.

Замазано **7 номерів на 3 фото**. **Пікселізація, не розмиття** — розмиття
частково піддається відновленню.

Координати шукав не на око: наклав координатну сітку на фото і зчитав межі.
Перші оцінки були хибні — кроп показав не ту машину.

### 🪤 Конфлікт після squash-merge
`git merge origin/main` дав конфлікт у `index.html`, `style.css`, `i18n.js`.
Звірка `git diff origin/main:файл c15c46a:файл` показала: файли
**побайтово ідентичні**, конфліктувала лише історія. Узято версію гілки;
`NOW.md` — з `main` (там свіжіший опис публікації).
**Правило спрацювало:** не глушити мерж через `-X ours/theirs`, а звірити
кожен файл окремо.

### 3. Instagram
Посилання вже стояло у 6 місцях. Вова дав адресу з хвостом
`?igsi=eDE1eWtiazF3enVw` — трекінговий параметр його шеру. Прибрано,
решту приведено до `https://www.instagram.com/t.k.performance`.

### ➡️ Досі чекає Вови
1. **Підписи під фото** — зараз нейтральні, бо не знаю, що це були за
   роботи. «N47 timing chain replacement» продає компетенцію краще, ніж
   «Engine stripped down on the bench».
2. **Telegram** — форма поки чесно каже подзвонити.
3. **Вичитати англійські тексти** — перелік двигунів і послуг.
4. **Відгуки** — не вигадував, секції немає навмисно.
