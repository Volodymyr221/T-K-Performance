# Журнал сесії — 2026-09-05

**Гілка:** `claude/startuem-kylhz0` · **старт:** `origin/main` = `b5f9147`

---

## Крок 0 — старт

- `git config user.name/email` → Вова (коміти підписані ним).
- `git fetch origin` — свіжий стан узятий з `origin`, не з диска.
- `git log HEAD ^origin/main` — **порожньо**, гілка стоїть на верхівці `main`.
- Робоче дерево чисте.

## Крок 1 — прочитано

`CLAUDE.md` · `NOW.md` · `SKILLS_README.md` — обовʼязкові три, більше нічого
підряд не читав (решта — через `Grep` за темою).

## Стан на старті

Сайт живий, **11 PR злито**. `NOW.md` має зріз на 2026-09-03 — на день старіший
за останні два коміти в `main` (PR #11: сірий напис на кнопці в меню).

---

## Хід сесії

_(дописується по ходу)_

### Нова велика задача — внутрішня CRM для СТО

**Слова Вови (стисло, суть дослівно):** «зробити внутрішню CRM-систему…
тільки в адмінів, у власників, у працівників… контролювати заявки, бюджет,
хто скільки заплатив, легше вести аудит… ми будемо їм заливати рекламу,
приводити людей за свої кошти, і вони будуть нам за цих людей [платити]…
клієнт заплатив тисячу доларів за ремонт і 10% нам, це 100 доларів… потрібно,
щоб у цій CRM була позначка, що прийшов з сайта, чи заявка прилетіла з сайта,
чи можна було вручну встановити ще, що це з сайта… якщо цей клієнт захоче
прийти другий раз і вони його запишуть… то було вказано, що цей клієнт з
сайта прийшов, типу з реклами… щоб їм легше було вести облік клієнтів,
аудит, хто має заїхати, на яку годину… місячний звіт, річний звіт…
в стилі сайту».

**Партнерство:** Вова + Діма знайшли клієнта. Модель — сайт + реклама за наш
кошт, СТО платить **10% від чека** за приведеного клієнта.

**Статус:** обговорення. Коду не чіпав — «Роби» не було.

### Референс CRM — макет опубліковано

**Вова:** «Можеш зробити референс CRM для тел і компʼютера».

Зробив макет (не код продукту — правило 2 не порушено, «Роби» ще не було).
Опубліковано як артефакт: https://claude.ai/code/artifact/5e985717-632b-48dd-9be8-ca63f66ba50d

**Що всередині:**
- Комп'ютер: `Today` (розклад + нові заявки), картка клієнта (атрибуція +
  журнал змін джерела), `Report` (спільний звіт з панеллю «Not counted»).
- Телефон: `Today`, `Close job`, пошук клієнта за номером.

**Рішення, закладені в макет (мої припущення, не узгоджені):**
- Токени взяті один в один з `assets/css/style.css` — жодного нового кольору.
- Синій BMW M зарезервований ВИКЛЮЧНО під атрибуцію. Статуси — окремі
  приглушені зелений/бурштиновий. Одна фарба — один сенс.
- Механік на телефоні НЕ бачить комісії: вводить лише labour і parts.
- Поля «скільки платити партнерам» не існує — комісія обчислюється.
- Панель «Not counted» у звіті: що НЕ порахували і чому. Головна панель довіри.
- Ставки 10% labour + 4% parts, вікно 12 міс, поріг £150 — плейсхолдери.
- Мова інтерфейсу — англійська британська (£, MOT, dd/mm).

🪤 **Дрібниця:** відсоткова висота стовпчиків у флекс-колонці з підписом
над нею переповнює контейнер — стеля 80%, не 100%.

### Референс → українська + перемикач мови

**Вова:** «переклади інтерфейс на українську · Треба також добавити перемикач
на укр/анг · Але тут головна мова українська».

Зроблено, та сама адреса артефакту (посилання не змінилось).

- Головна мова — українська, за замовчуванням. `setLang('ua')` на старті.
- Перемикач **у двох місцях**: справжній угорі сторінки-референсу (перемикає
  всі 6 екранів) і намальований усередині CRM — у лівій колонці десктопа та
  вгорі екрана `Сьогодні` на телефоні.
- Механізм: `data-ua` / `data-en` на елементах, JS міняє `textContent`,
  на `<html>` ставиться `data-lang` і `lang` (`uk` / `en-GB`).
- Manrope доданий у завантаження шрифтів — Archivo не має кирилиці.
  Той самий прийом, що вже працює на сайті, нового шрифту не додано.

**Додано розділ 03 «Що лишається англійською завжди»** — таблиця:
MOT · £ і `dd/mm/yyyy` · номерні знаки · імена клієнтів · індекси BMW
(F30, N47) · текст заявки клієнта дослівно · 🔴 усе, що виходить назовні
(invoice, SMS, email).

🪤 **Головне, що я витягнув у макет:** мова інтерфейсу ≠ мова документів.
Це два різні перемикачі, і другого в інтерфейсі немає взагалі. Механік
працює українською, але на екрані «Закрити роботу» під кнопкою прямо
написано «Рахунок клієнту піде англійською» — інакше британцю прилетить
українська накладна.

Терміни й ставки на екранах — і далі МОЇ припущення, не узгоджені.

### «Роби» на схему бази → `crm/SCHEMA.md`

**Вова:** «Роби» (на схему бази даних).

Перед тим Вова відповів на три питання: бюджет — **$0**; про репозиторій і
пошту перепитав, що це означає — пояснив, рішення ще за ним.

Створено `crm/SCHEMA.md`. Перевірив `.github/workflows/pages.yml` — публікується
явний список файлів, тека `crm/` на сайт не потрапляє.

**Таблиці:** `staff` · `clients` · `vehicles` · `enquiries` · `bookings` ·
`jobs` · `commission_settings` · `commissions` · `periods` · `audit_log` ·
`app_settings`.

**Рішення, які варто памʼятати:**
- 🔴 **Клієнт = телефон** (`UNIQUE` на E.164). Наслідок: сімʼя з одним номером —
  один клієнт. Без цього атрибуція розсипається на дублікатах.
- 🔴 **`attribution_expires_at` зберігається, а не обчислюється.** Зміна вікна
  не має зсувати вже наявних клієнтів. Те саме зі ставками в `commissions` —
  зберігається знімок ставки на момент нарахування.
- 🔴 **Рядок `commissions` створюється ЗАВЖДИ**, навіть коли комісії немає —
  зі `status='excluded'` і причиною. Саме тому панель «Що НЕ рахували» у звіті
  не порожня: це записані відмови, а не відсутність даних.
- 🔴 **Роль `partner`** (ми з Дімою) НЕ бачить персональних даних клієнтів —
  лише цифри й «J.W. · …318». Знімає страх СТО, знімає GDPR-ризик з нас.
- `bookings.all_day` — схема витримує і запис на годину, і «привозь у вівторок».
  Тому питання «як вони записують» більше не блокує роботу.
- `jobs` окремо від `bookings`: не кожна робота має запис, не кожен запис —
  роботу.
- Гроші — `numeric(10,2)`, ніколи не float.
- Ставки — у таблиці `commission_settings` з `effective_from`, не в коді.
  Тому відкладене рішення Вови про % нічого не блокує.

🪤 **GDPR vs HMRC:** право на видалення конфліктує з вимогою зберігати
фінансові записи 6 років. Рішення — **анонімізація** персональних полів
(телефон → `deleted:<рандом>`, щоб UNIQUE вцілів), суми лишаються.

**NOW.md оновлено:** додано розділ про CRM, зріз на 2026-09-05. 152 рядки —
під стелею 200.

### «Роби» → міграції написані Й ПЕРЕВІРЕНІ на живому Postgres

**Вова:** «Srm наша, ми їм даємо дозвіл на користування з їхньою базою · Роби».

🔴 **Ключове рішення партнерства:** система наша, СТО користується за дозволом,
дані їхні. Це найсильніша позиція — важіль лишається в нас.

Раніше в цьому ж повідомленні Вова відповів: пошту зроблять власники (не чекаємо,
стартуємо на тестовій базі); репозиторій поки цей, приватним зробимо потім;
власники на CRM погодились.

**Створено:**
- `crm/migrations/0001_init.sql` — типи, 11 таблиць, індекси
- `crm/migrations/0002_logic.sql` — нормалізація телефону/номера, вікно
  атрибуції, розрахунок комісії, аудит, закриття місяця, анонімізація
- `crm/migrations/0003_rls.sql` — права доступу + `partner_report`,
  `period_summary`
- `crm/migrations/0004_seed_dev.sql` — вигадані дані (🛑 тільки тестова база)
- `crm/tests/rls_test.sql` — злий тест прав
- `crm/README.md`

**🟢 ПЕРЕВІРЕНО НА СПРАВЖНІЙ БАЗІ** (PostgreSQL 16.13, тимчасовий кластер):
- 4 міграції застосувались, exit 0;
- комісії ніхто не вставляв — їх нарахував тригер:
  `due 7 / £454.60`, і всі 4 види виключень спрацювали
  (below_threshold 1 · not_our_source 2 · pre_existing 1 · window_expired 1);
- нормалізація телефону: 4 різні формати → `+447700900318`;
- вікно атрибуції +12 міс проставилось само;
- **злий тест прав: 28/28 PASS.** Механік не дістав комісій, приймальник
  не дістав ставок, партнер не дістав імен, анонім не дістав нічого.

**Що бачить партнер замість імені:** `J.W · 318 · N47 timing chain · 124.80 · due`.

🪤 **Пастки, на які наступив і виправив:**
- `to_char()` не `IMMUTABLE` → індекс на `to_char(done_on,'YYYY-MM')`
  Postgres будувати відмовляється. Прибрав, вибірка йде діапазоном дат.
- `audit_log.entity_id` був `not null`, але подія «закрито місяць»
  стосується періоду, а не рядка → зробив nullable.
- **Власник таблиць обходить RLS завжди.** Перший прогін тесту показував
  FAIL/PASS хаотично, бо `SET LOCAL role` поза транзакцією мовчки не діє —
  усе виконувалось від суперкористувача. Тестувати треба під окремою
  роллю `app_user`.
- `psql` без `ON_ERROR_STOP=1` повертає **0 на помилці** — перший прогін
  показав «exit: 0» на чотирьох зламаних міграціях.
- Обом партнерам у тесті дістався один `auth_user_id` → unique violation
  відкотив увесь UPDATE, і всі ролі стали null.

**Синхронізовано:** `SCHEMA.md` описував `staff.id references auth.users`,
а міграція має окремий nullable `auth_user_id` (щоб міграція лягала і на
чистий Postgres, і її можна було перевірити локально). Документ виправлено —
інакше він брехав би.

**Не зроблено цього кроку:** інтерфейс на фальшивих даних — наступний.
